name: "Custom CodeQL Analysis for Java"
queries:
  - uses: +security-and-quality
paths:
  - src/  # Quét tất cả file trong src/ (các file .java)
paths-ignore:
  - '**/*.test.*'  # Bỏ qua file test nếu có
